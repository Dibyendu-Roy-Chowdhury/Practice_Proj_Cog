apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: veriforgeops
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 8080  # pod listens on 8080 (nginx-unprivileged); Service exposes 80 externally
    protocol: TCP
